const readLine = require('readLine')

const rl = readLine.createIntrface({
    input: process.stdin,
    output: process.stdout
})
Math.floor(Math.random() * 100)
function solictarpalpite() {
    rl.question('')
}